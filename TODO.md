# TODO - احترافية ودقة وقابلية تركيب

## الخطوة 1
- [x] إعادة تنظيم `app.js` ليصبح “مكوّن” قابل للتركيب: `createVoiceMedicalForm(root, options)`.
- [x] إزالة الاعتماد على IDs الثابتة (الاعتماد على عناصر داخل الـ root + data-attributes).


## الخطوة 2
- [ ] تحسين معالجة التصحيح: جعل التصحيح يعمل مع الترقيم (tokens تشمل أحرف + علامات).

## الخطوة 3
- [ ] استبدال زر “مراجعة وحفظ” بآلية “تجهيز نص للإرسال” فقط (callback/حدث custom) بدون تخزين.

## الخطوة 4
- [ ] توسيع `styles.css` بأسماء namespace مثل `mmf-` + CSS variables.

## الخطوة 5
- [ ] تحديث `index.html` ليصبح صفحة Demo فقط تستخدم المكوّن.

## الخطوة 6
- [ ] اختبار يدوي: RTL + زر المايك + التصحيح + تشغيل/إيقاف المايك بدون أخطاء.

